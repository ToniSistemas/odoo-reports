<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Inherit Invoice Form View to Add Payment Receipt A5 Button -->
    <record id="view_move_form_inherit_payment_receipt_a5" model="ir.ui.view">
        <field name="name">account.move.form.inherit.payment.receipt.a5</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Add button directly inside the header -->
            <xpath expr="//header" position="inside">
                <button name="action_print_payment_receipt_a5"
                        string="Imprimir Comprobante A5"
                        type="object"
                        class="btn-secondary"
                        groups="base.group_user"
                        condition="move_type in ('out_invoice', 'out_refund')"/>
            </xpath>
        </field>
    </record>
</odoo>
